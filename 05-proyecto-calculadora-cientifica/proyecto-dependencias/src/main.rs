use regex::Regex;

fn main() {
    // Regex
    // Regex::new(r"^\d{2}\.\d{2}$")
    // Regex::new(r"\d+) \s? \\d{2}$").unwrap()

    // Traemos los datos del usuario

    // Hacemos las operaciones

    // Por ultimo mostramos los resultados
}
